{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "goal": {
      "type": "string",
      "minLength": 1,
      "description": "High-level user goal for Alpha. Keep bulky documents, parsed PDF text, logs, generated files, and archives out of this string; pass them through TaskEnvelope.input_artifacts or delegate_to_agent.artifact_ids so Alpha can inspect staged files directly. If context comes from a parsed bundle, pass the source artifact reference rather than only a bundle_id."
    },
    "mode": {
      "type": "string",
      "enum": ["auto", "new_project", "existing_project"],
      "default": "auto",
      "description": "Whether Alpha should create a new project, use an existing project, or infer automatically."
    },
    "project_ref": {
      "type": ["string", "null"],
      "description": "Existing Alpha project id, alias, repo URL, deployment URL, local path, prior task id, artifact id, or natural-language project/task reference."
    },
    "aliases": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional aliases for a newly created Alpha project."
    },
    "repo_url": {
      "type": ["string", "null"],
      "description": "Repository URL associated with a new Alpha project."
    },
    "deployment_url": {
      "type": ["string", "null"],
      "description": "Deployment URL associated with a new Alpha project."
    },
    "preferred_harness": {
      "type": "string",
      "enum": ["codex", "opencode", "cursor", "auto"],
      "default": "codex",
      "description": "Preferred execution harness. Codex, Cursor, and auto are currently executable."
    },
    "preferred_model": {
      "type": ["string", "null"],
      "description": "Optional CLI model override for this task."
    },
    "model": {
      "type": ["string", "null"],
      "description": "Optional CLI model override for this task."
    },
    "codex_sandbox": {
      "type": "string",
      "enum": ["read-only", "workspace-write", "danger-full-access"],
      "default": "danger-full-access",
      "description": "Codex sandbox mode for model-generated shell commands."
    },
    "allow_cross_harness_fallback": {
      "type": "boolean",
      "default": false,
      "description": "When true, Alpha may continue the same workspace with the other authenticated CLI harness after same-provider retries are exhausted. Default is false so a selected provider such as Cursor is retried and diagnosed before any provider switch."
    },
    "allow_harness_fallback": {
      "type": "boolean",
      "default": false,
      "description": "Legacy alias for allow_cross_harness_fallback. Default is false; do not set true unless cross-provider continuation is explicitly acceptable."
    },
    "max_harness_attempts": {
      "type": "integer",
      "minimum": 1,
      "maximum": 3,
      "default": 2,
      "description": "Maximum attempts with the selected CLI harness before failing or, only when allow_cross_harness_fallback=true, trying another authenticated harness."
    },
    "require_preferred_harness": {
      "type": "boolean",
      "default": false,
      "description": "When true, Alpha must not fall back to another CLI harness. Use only for provider diagnostics or user-mandated provider runs."
    },
    "deliverables": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Requested final deliverables."
    },
    "approval_policy": {
      "type": "string",
      "enum": ["container_autonomous", "ask_before_side_effects", "ask_every_step"],
      "default": "container_autonomous",
      "description": "Future approval posture for Alpha execution."
    },
    "constraints": {
      "type": "object",
      "description": "Optional structured constraints for the project task."
    },
    "run_workspace_smoke": {
      "type": "boolean",
      "default": false,
      "description": "Run a harmless Docker workspace smoke test only when ALPHA_ALLOW_DOCKER_SMOKE=true."
    },
    "prepare_only": {
      "type": "boolean",
      "default": false,
      "description": "Prepare or recall the Alpha workspace without running the selected CLI."
    }
  },
  "required": ["goal"]
}
