{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "project_ref": {
      "type": ["string", "null"],
      "description": "Project id, alias, repo URL, deployment URL, local path, prior task id, artifact id, or natural-language project/task reference to recall."
    },
    "query": {
      "type": ["string", "null"],
      "description": "Optional natural-language search query across Alpha project and task metadata. Used when project_ref is absent."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    }
  }
}
